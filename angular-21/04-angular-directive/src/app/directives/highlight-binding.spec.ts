import { HighlightBinding } from './highlight-binding';

describe('HighlightBinding', () => {
  it('should create an instance', () => {
    const directive = new HighlightBinding();
    expect(directive).toBeTruthy();
  });
});
