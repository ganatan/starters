import { HighlightRenderer } from './highlight-renderer';

describe('HighlightRenderer', () => {
  it('should create an instance', () => {
    const directive = new HighlightRenderer();
    expect(directive).toBeTruthy();
  });
});
